services:
  nginx-rtmp:
    image: tiangolo/nginx-rtmp:latest
    container_name: gm-nginx-rtmp
    ports:
      - "1935:1935"
      - "8080:8080"
    volumes:
      - ./infra/nginx-rtmp/nginx.conf:/etc/nginx/nginx.conf:ro
      - rtmp-hls:/tmp/hls
    restart: unless-stopped

volumes:
  rtmp-hls:
